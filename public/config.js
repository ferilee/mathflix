window.APP_CONFIG = {
  // Leave empty to use VITE_API_URL from .env
  API_URL: "",
  // "cookie" (session-based) or "token" (bearer token in Authorization header)
  AUTH_STRATEGY: "cookie",
  // Optional: base URL for /storage assets (defaults to API_URL)
  STORAGE_URL: "",
};
