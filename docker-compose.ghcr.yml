services:
  mathflix-frontend:
    image: ghcr.io/ferilee/mathflix:latest
    container_name: mathflix-frontend
    ports:
      - "9001:80"
    environment:
      - VITE_API_URL=http://api-mathflix:3000
    restart: unless-stopped
