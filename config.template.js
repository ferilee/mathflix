// Runtime configuration file
window.APP_CONFIG = {
  API_URL: "{{API_URL}}" || "http://localhost:3000",
  ADMIN_USERNAME: "{{ADMIN_USERNAME}}" || "ferilee",
  ADMIN_PASSWORD: "{{ADMIN_PASSWORD}}" || "F3r!-lee",
};
